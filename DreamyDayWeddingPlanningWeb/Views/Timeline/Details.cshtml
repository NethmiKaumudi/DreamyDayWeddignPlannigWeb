@model DreamyDayWeddingPlanningWeb.Models.TimelineEvent

@{
    ViewData["Title"] = "Timeline Event Details";
    Layout = "~/Views/Shared/_CoupleLayout.cshtml";
}

<h2 class="mb-4 mt-4 text-center" style="font-size: 24px; color: #333;">Timeline Event Details</h2>

<div class="container-fluid">
    <div class="card">
        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger" role="alert">
                @TempData["ErrorMessage"]
            </div>
        }
        else
        {
            <h2 class="mb-4 mt-4 text-center" style="font-size: 20px; color: #333;">Timeline Event Details</h2>
            <hr class="mb-3" />

            <dl class="row">
                <dt class="col-sm-3">Wedding ID</dt>
                <dd class="col-sm-9">
                    <input type="text" class="form-control" value="@ViewBag.WeddingId" readonly />
                </dd>
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.EventName)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.EventName)</dd>
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.StartTime)</dt>
                <dd class="col-sm-9">@Model.StartTime.ToString("hh:mm tt, MMM dd, yyyy")</dd>
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.EndTime)</dt>
                <dd class="col-sm-9">@Model.EndTime.ToString("hh:mm tt, MMM dd, yyyy")</dd>
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Description)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Description)</dd>
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.IsDeleted)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.IsDeleted)</dd>
            </dl>

            <div>
                <a asp-action="Edit" asp-route-id="@Model?.Id" asp-route-weddingId="@ViewBag.WeddingId" class="btn btn-primary">Edit</a>
                <a asp-action="Index" class="btn btn-secondary">Back to List</a>
            </div>
        }
    </div>
</div>

<style>
    .container-fluid {
        width: 100%;
        padding: 15px;
        height: 100%;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .card {
        background-color: #fff;
        border: 1px solid #ddd;
        border-radius: 8px;
        padding: 15px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 500px;
    }

        .card h2 {
            font-size: 20px;
            color: #333;
            margin-left: 15px;
        }

    .row {
        margin: 0;
    }

    .col-sm-3 {
        font-weight: bold;
        color: #333;
    }

    .col-sm-9 {
        color: #555;
    }

    .form-control[readonly] {
        background-color: #f8f9fa;
        cursor: not-allowed;
    }

    .btn-primary {
        background-color: #007bff;
        border: none;
        padding: 8px 15px;
        color: #fff;
        border-radius: 5px;
    }

        .btn-primary:hover {
            background-color: #0056b3;
        }

    .btn-secondary {
        background-color: #6c757d;
        border: none;
        padding: 8px 15px;
        color: #fff;
        border-radius: 5px;
        margin-left: 10px;
    }

        .btn-secondary:hover {
            background-color: #5a6268;
        }

    .alert {
        margin-bottom: 15px;
    }
</style>